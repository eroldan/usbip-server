[Unit]
Description=Starts a humble web application to manage usbip
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=/opt/usbip-server/web
ExecStart=/usr/bin/flask run --host=0.0.0.0 --port=5000
Restart=on-failure

[Install]
WantedBy=multi-user.target